**本文档旨在说明机场地面车辆管理系统在建模过程中所做的主要简化决策**。机场真实地面保障业务高度复杂，涉及数十种特种车辆、精细的机型匹配规则和复杂的任务协调机制。为使系统在作业范围内具备可操作性和可理解性，我们在现实依据的基础上进行了一系列有意识的简化。

以下逐一说明各项简化所面对的现实复杂性、最终采用的简化方式以及选择理由。

## 机场布局建模



## 机型选择与建模

硕放机场实际运行的机型包括A320系列、B737系列、A330等，也存在货机、公务机等非定期航班。不同机型的尺寸、重量、舱门高度各异，对车辆的要求也不同。我们最终选择只考虑A320、B737、A330、B777等5种常见客机，主动排除了货机和其他非定期航班机型。主要理由如下：

- **保障需求差异大**：货机不涉及旅客服务，其地面保障作业中完全不需要客梯车、摆渡车和客舱清洁车，却可能需要大型升降平台车等专用设备。若两类同时考虑，会导致车辆管理系统膨胀，调度难度复杂。
- **任务生成逻辑分离**：客运航班与货运航班的保障任务清单存在根本性不同，强行统一建模会使任务生成规则变得臃肿且充满特例。
- **验证系统逻辑功能所需的代表性以足够**：四种机型完整覆盖了窄体机和宽体机两大类别，足以验证车辆与机型的匹配逻辑，同时避免因机型过多导致规则表膨胀和测试数据失控。


## 车辆类型选择

为完成完整的地面服务作业，机场特种车辆多达数十种，包括油罐车、加油栓车、行李传送带车、行李拖车、除冰车、清水车、污水车、配餐车等、各型客梯车等。这其中存在如下多样性：

**车辆用途归属不同**：机场地面车辆并非全部服务于航班过站保障。例如，场道维护车辆负责跑道和滑行道的巡检与修补；除雪车和吹雪车仅在冬季恶劣天气出动，保障道面适航性；这些车辆的调度逻辑、出动条件和任务触发机制与航班过站保障车辆存在根本性不同。

**实现相同保障功能的技术路线不同**，比如为提供加油服务，小型机场多提供油罐车，往返于油库和机位之间；大型枢纽机场则多采用地井式加油，即停机位附近存在加油井，此时需一辆相对紧凑的加油栓车开到飞机附近，启动自身搭载的泵、过滤器和计量系统，将油料从地井被吸入车辆，经过加压和过滤后，注入飞机油箱。

**服务能力等级不同**：同一类车辆，由于服务对象不同，在规格和性能上存在显著差异。以客梯车为例，服务窄体机（如A320/B737）的标准客梯车工作高度一般不超过4.4米，而服务宽体机（如A330/B777）则需要高升程客梯车，工作高度需达到5.8米以上。类似地，牵引车按牵引力等级分为小型（120-160kN）、中型（180-240kN）和大型（300-400kN），分别对应不同最大起飞重量的机型；电源车则按输出功率分为90kVA和140kVA两档。

**机位基础设施差异**：同一项地面服务，在不同机位上的实现方式不同。廊桥机位大多集成了旅客通道、电力接口、空调接口，因此客梯车、摆渡车、清洁车无需出动，提供清洁服务人员经廊桥即可进出客舱。远机位则完全依赖车辆保障，上述三种车辆必须全部到位。这一差异意味着，系统必须将机位类型作为任务生成和车辆派遣的关键判断变量，而非一概而论地生成全部任务。

**服务对象的需求可变性**：并非所有保障任务都是每个航班必须执行的。牵引和电源供应属于刚性需求，几乎所有离港航班都需要；行李装卸对客运航班不可或缺；客舱清洁在过站期间基本都会执行，但加油则完全取决于航司的航程计划和剩余油量。因此，任务生成逻辑需要区分这两种情况。

**车辆组合与协同模式不同**：部分保障任务需要多车协同，协同模式又因场景而异。如满载的A330或B777需要数辆摆渡车才能在合理时间内完成旅客运输。牵引车和电源车则通常一车一机即可完成任务。这意味着系统必须根据机型和任务量动态计算所需车辆数量。

针对上述多维度复杂性，本系统对车辆种类做出以下简化决策：

将建模范围暂时限定为**直接参与航班过站保障的车辆**，即围绕一架特定飞机在经停时间内完成特定服务任务的车辆。不考虑场道维护车车辆，消防车等。本系统的定位是，为了提高调度效率，而构建的航班过站保障车辆**调度管理系统**；场道维护车辆，消防车等车辆，在大多数时候不影响航班任务的推进，若将其纳入考量，那么监控系统的复杂度还会上升，故将其排除。

在此基础上，将**核心车辆类型收敛为六种**，仅保留地面保障链中不可替代或最具代表性的车辆类型：

- 牵引车：牵引飞机推出停机位或拖行至指定机位，是所有离港及机位调整航班的刚性需求车辆。
- 电源车：在飞机停场期间（尤其是APU关闭时）为飞机提供地面电力，保障航电系统、照明和空调运行，是所有停场航班的刚性需求车辆。
- 客梯车：供旅客通过客梯车上下飞机。廊桥机位由廊桥替代此功能，故仅远机位生成该任务。
- 摆渡车：负责在航站楼与远机位之间运送旅客。廊桥机位旅客经廊桥直接通行，故仅远机位生成该任务。
- 加油车：设定本机场采用加油栓式加油系统，加油车连接机位地井与飞机，完成燃油加注。加油任务按需生成，非每个航班必须。
- 行李拖车：负责将行李在机位与航站楼行李处理区之间进行水平运输

其余车辆不予考虑的原因：

- 客舱清洁排污等过站服务，通常由外包公司负责，且服务模式不统一；
- 空调车：和电源车的使用场景类似；
- 飞机除冰/除雪车、机身清洗车：使用频率低；
- 行李传送带车/叉车（将货舱的行李运到行李拖车）：需和行李拖车配合完成工作，二者功能紧密耦合，不单独建模。

## 机场车辆配置

牵引车：

| 型号    | 生产商   | 牵引力 | 适用机型   | 数量 |
| :------ | :------- | :----- | :--------- | ---- |
| WGQY-20 | 威海广泰 | 160 kN | A320, B737 | 5    |
| TMX-350 | TLD      | 350 kN | A330, B777 | 1    |

电源车（仅远机位需要）：

| 型号     | 生产商   | 输出功率 | 适用机型   | 数量 |
| :------- | :------- | :------- | :--------- | :--- |
| WGJD-90  | 威海广泰 | 90 kVA   | A320, B737 | 3    |
| WGJD-180 | 威海广泰 | 140 kVA  | A330, B777 | 2    |

客梯车（仅远机位需要）：

| 型号    | 生产商   | 最大工作高度 | 适用机型   | 数量 |
| :------ | :------- | :----------- | :--------- | :--- |
| WGKT-44 | 威海广泰 | 4.4m         | A320, B737 | 2    |
| WGKT-58 | 威海广泰 | 5.8m         | A330, B777 | 1    |

旅客摆渡车（仅远机位需要）：

| 型号     | 生产商   | 载客量 | 适用机型 | 数量 |
| :------- | :------- | :----- | :------- | :--- |
| ZK6140BD | 宇通客车 | 110人  | 通用     | 3    |

加油车（加油栓式）

| 型号      | 生产商   | 类型     | 适用机型 | 数量 |
| :-------- | :------- | :------- | :------- | :--- |
| CF-GL-250 | 上海承飞 | 加油栓车 | 通用     | 4    |

行李拖车：

| 型号    | 生产商   | 牵引能力 | 适用机型 | 数量 |
| :------ | :------- | :------- | :------- | :--- |
| WGDT-25 | 威海广泰 | 25吨级   | 通用     | 8    |


### 分区车辆配置

A 区全部为廊桥机位，B 区含 4 个远机位（B05-B08）。宽体机（A330、B777）只能停靠远机位，窄体机（A320、B737）优先停靠廊桥机位。仅远机位需要的车辆（电源车、客梯车、摆渡车）全部部署在 B 区。

**A 区（1 号航站楼，全部廊桥）**

| **类型** | **型号**           | **等级** | **数量** | **说明**               |
| :----- | :----------------- | :----- | :--- | :--------------------- |
| **TOW** | **威海广泰 WGQY-20** | **小型** | **3** | **窄体机牵引**         |
| **FUEL** | **上海承飞 CF-GL-250** | **标准** | **2** | **加油栓车**           |
| **BAG** | **威海广泰 WGDT-25** | **标准** | **4** | **行李拖车**           |
| **合计** |                    |        | **9** |                        |

**B 区（2 号航站楼，含 B05-B08 远机位）**

| 类型   | 型号               | 等级   | 数量 | 说明                         |
| :----- | :----------------- | :----- | :--- | :--------------------------- |
| **TOW** | **威海广泰 WGQY-20** | **小型** | **2** | **窄体机牵引**               |
| TOW    | TLD TMX-350        | 大型   | 1    | 宽体机牵引，仅远机位         |
| **GPU** | **威海广泰 WGJD-90** | **小型** | **3** | **90kVA 电源车，仅远机位**   |
| GPU    | 威海广泰 WGJD-180  | 大型   | 2    | 140kVA 电源车，仅远机位      |
| **STAIR** | **威海广泰 WGKT-44** | **标准** | **2** | **4.4m 客梯车，仅远机位**    |
| STAIR  | 威海广泰 WGKT-58   | 高升程 | 1    | 5.8m 客梯车，仅远机位        |
| BUS    | 宇通 ZK6140BD      | 大型   | 3    | 110 人摆渡车，仅远机位       |
| FUEL   | 上海承飞 CF-GL-250 | 标准   | 2    | 加油栓车                     |
| BAG    | 威海广泰 WGDT-25   | 标准   | 4    | 行李拖车                     |
| **合计** |                    |        | **20** |                             |



## 系统实现：以数据库为中心的分层架构

本系统的数据分为两个角色：JSON 文件作为团队协作的数据交换格式与种子数据来源，MySQL 数据库作为唯一的运行时数据源。系统启动时通过 `seed.py` 将 JSON 数据导入数据库，运行期间所有模块只读写数据库，不直接依赖 JSON 文件。

### JSON 文件 —— 数据交换与导入格式

JSON 文件用于团队成员之间共享和版本管理参考数据，通过 `seed.py` 一次性导入数据库：

- `aircraft_catalog.json`：各机型的物理与技术参数（以机型为对象）。导入后对应数据库 `aircraft_models` 表。
- `vehicle_models.json`：各品牌车辆型号的技术参数与适用机型（以车辆型号为对象）。导入后对应数据库 `vehicle_models` 表。
- `vehicle_inventory.json`：本机场实际配备的车辆清单，每台车记录其牌照号、车辆型号、购置日期、固定资产标签等（以车辆个体为对象）。导入后对应数据库 `vehicle_info` 表。
- `airport_physical.json`：机场物理布局（区域、停机位属性、路网拓扑）。导入后对应 `regions`、`gates`、`road_nodes`、`road_edges` 表。
- `airport_display.json`：态势图显示坐标。导入后对应 `gate_display` 表。

团队成员修改参考数据时，更新对应的 JSON 文件并提交到版本库，其他成员拉取后重新运行 `seed.py` 即可同步。

### MySQL 数据库 —— 统一的运行时数据源

运行期间所有数据均存储在 MySQL 中，所有模块（任务生成、调度分配、路径计算、态势图渲染）只通过数据库读写数据：

**参考数据表**（结构稳定，随种子数据更新）：

- `aircraft_models` 表：机型技术参数，含 `type`（A320 / B737 等）、`dimensions`（JSON）、`weight`（JSON）、`capacity`（JSON）、`technical`（JSON），供任务生成与车辆匹配时查询。
- `vehicle_models` 表：车辆型号技术参数，含 `vehicle_type`（TOW / GPU / STAIR / BUS / FUEL / BAG）、`brand_model`（型号名称）、`specs`（JSON，牵引力、最大高度、容量等）、`compatible_aircraft`（适用机型列表）。调度器据此验证车辆型号是否满足目标机型要求。
- `vehicle_info` 表（车辆静态信息）：以车辆个体为对象，记录本机场实际配备的每台车。含 `plate_number`（主键）、`vehicle_type`、`brand_model`（关联 vehicle_models）、`purchase_date`、`asset_tag`、`remark`。
- `regions` 表：航站楼区域划分。
- `gates` 表：停机位信息，含 `code`、`region_id`、`has_jet_bridge`（决定是否需要客梯车与摆渡车）。
- `road_nodes` / `road_edges` 表：路网拓扑，含节点类型（INTERSECTION / SERVICE_STATION / GATE）及路段距离与通行时间。

**运行动态表**（数据随航班进出港持续变化）：

- `flights` 表：`flight_no`、`airline`、`aircraft_type`（关联 aircraft_models）、`scheduled_at`、`arrival_at`、`gate_id`、`status`（SCHEDULED / ARRIVED / DEPARTED / CANCELLED）。
- `tasks` 表：`task_type`（TOW / GPU / STAIR / BUS / FUEL / BAG）、`status`（PENDING / IN_PROGRESS / COMPLETED / FAILED）、`depends_on`（前置任务 ID）。
- `vehicle_status` 表（车辆实时状态）：`plate_number`（外键关联 vehicle_info 主键）、`current_status`（IDLE / ASSIGNED / BUSY / MAINTENANCE）、`current_location`、`current_task`、`assigned_aircraft`。

### 数据流

```
┌──────────────────────┐         ┌──────────────────────────────┐
│    JSON 文件           │         │      MySQL 数据库              │
│  （数据交换格式）        │         │    （运行时唯一数据源）           │
├──────────────────────┤         ├──────────────────────────────┤
│ aircraft_catalog.json  │ ──导入→ │ aircraft_models              │
│ vehicle_models.json    │ ──导入→ │ vehicle_models               │
│ vehicle_inventory.json │ ──导入→ │ vehicle_info                 │
│ airport_physical.json  │ ──导入→ │ regions / gates / road_*     │
│ airport_display.json   │ ──导入→ │ gate_display                 │
└──────────────────────┘         ├──────────────────────────────┤
                                 │ flights / tasks               │
                                 │ vehicle_status                │
                                 │ ← 运行时持续读写 ──────────────│
                                 └──────────────────────────────┘
```

JSON 文件仅在 `seed.py` 执行时被读取，运行期间所有模块（任务生成、调度分配、路径计算、态势图 API）均直接查询 MySQL。团队通过更新 JSON 文件并重新运行 `seed.py` 来同步参考数据变更。

## reference

[2025中国民航机队观察_澎湃号·政务_澎湃新闻-The Paper](https://www.thepaper.cn/newsDetail_forward_32406182)